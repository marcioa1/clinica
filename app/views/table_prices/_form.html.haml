= form_for(@table_price) do |f| 
  - if @table_price.errors.any? 
    #error_explanation
      %h2
        = pluralize(@table_price.errors.count, "error")  
        prohibited this table_price from being saved:

      %ul
        - @table_price.errors.full_messages.each do |msg| 
          %li
            = msg 
  
  .field
    = f.label :nome 
    = f.text_field :name 
  .actions
    = f.submit 'criar tabela de preços'
